import { StyleSheet, Text, View } from "react-native";
import React from "react";

const userInfoSetup = () => {
  return (
    <View>
      <Text>userInfoSetup</Text>
    </View>
  );
};

export default userInfoSetup;

const styles = StyleSheet.create({});
